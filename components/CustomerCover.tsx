"use client";

type CustomerProfile = { name: string; mobile: string; address: string };
type Props = { onContinue: (profile: CustomerProfile) => void };

export default function CustomerCover(_props: Props) {
  return null;
}
